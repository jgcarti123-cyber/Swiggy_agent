# Tweet draft

Single tweet + link. Portfolio/job-search framing — leads with the measured bottleneck + fix, not a feature list.

---

> I built a chat agent on Swiggy's new MCP servers to order groceries with plain text. First version worked but took 7 LLM calls and ~129s for one guided add.
>
> Profiled it, rebuilt the flow deterministic-first — model only where real judgment is needed. Down to 0-2 calls.
>
> https://github.com/jgcarti123-cyber/Swiggy_agent

(280 chars incl. link — check length in the actual Twitter compose box since link shortening varies; trim the second sentence first if it overflows: "Profiled it, rewrote it deterministic-first. Down to 0-2 calls.")

## Optional follow-up reply (if you want to thread it after posting, not required for the first post)

> Also caught it fabricating a nutrition fact for a fictional product during testing — the fix was a deterministic filter, not a bigger prompt. Docs + the whole debugging trail are in ARCHITECTURE.md in the repo.

## Notes
- Attach 1-2 of the screenshots from `docs/screenshots/` (the brand-picker one and the Explain one read best without extra context).
- Post timing: weekday morning US Eastern / early afternoon IST tends to get the most tech-Twitter eyes, but don't over-optimize for a first post — the content is the lever here, not the hour.
